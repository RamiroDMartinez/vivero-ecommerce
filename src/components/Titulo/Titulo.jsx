import React from "react"

const Titulo = ( { titulo, subTitulo } ) => {
    
    return (
        <div>
            <h2>{ titulo }</h2>
            <h4>{ subTitulo }</h4>
        </div>
    )
}
export default Titulo
